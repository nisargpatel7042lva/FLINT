/**
 * External-boundary modules: Health Connect, storage, HTTP clients.
 *
 * Nothing here yet. Health Connect integration is the reason this project uses
 * the React Native CLI rather than Expo, and it lands in a later phase.
 */

export {};
