/**
 * Shared React hooks.
 *
 * Note: `useTheme` deliberately lives in `src/theme`, next to the tokens it
 * serves. Re-exported here so hook consumers have one import site.
 */

export { useTheme, useThemeContext } from '../theme';
